<div class="page-header">
  <h2>New list</h2>
</div>

<%= simple_form_for(resource) do |f| %>
  <%= f.error_notification %>

  <%= render 'form', :f => f %>

  <div class="actions">
    <%= f.button :submit, "Create" %>
  </div>
<% end %>
